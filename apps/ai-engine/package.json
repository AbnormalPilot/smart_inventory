{
  "name": "ai-engine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bash -c '[ -d .venv ] || python3 -m venv .venv && source .venv/bin/activate && pip install -q -r requirements.txt && uvicorn src.main:app --reload --host 0.0.0.0 --port 8000'",
    "build": "echo 'No build step for Python service'",
    "setup": "python3 -m venv .venv && .venv/bin/pip install -r requirements.txt",
    "setup:ml": ".venv/bin/pip install torch --index-url https://download.pytorch.org/whl/cpu && .venv/bin/pip install chronos-forecasting",
    "lint": "echo 'No lint configured'"
  }
}
